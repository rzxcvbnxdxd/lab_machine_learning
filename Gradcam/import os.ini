import os
import sys
sys.path.append(r"C:\Users\ylab\Downloads\250327_新型チップ測定データ\250327_新型チップ測定データ\中間発表用解析\250821_123701\classification_file\cnn\250821_125241")
from network import Network

output_dir = r"C:\Users\ylab\Downloads\ダスキン研究データ検証_石川\S.aureus元波形"
os.makedirs(output_dir, exist_ok=True)

def get_label_name(label):
    return "E.coli" if label == 0 else "S.aureus"

for idx in range(x.shape[0]):
    label_name = get_label_name(y[idx])
    plt.figure(figsize=(8, 3))
    plt.plot(x[idx, :, 0], color='blue' if y[idx] == 0 else 'orange')
    plt.title(f'Waveform {idx:03d} ({label_name})')
    plt.xlabel('Time')
    plt.ylabel('Amplitude')
    img_path = os.path.join(output_dir, f"waveform_{idx:03d}_{label_name}.png")
    plt.tight_layout()
    plt.savefig(img_path)
    plt.close()
print(f"{x.shape[0]}個の波形画像を個別ファイルとして保存しました: {output_dir}")